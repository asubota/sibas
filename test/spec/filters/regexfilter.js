'use strict';

describe('Filter: regexFilter', function () {

  // load the filter's module
  beforeEach(module('sibasApp'));

  // initialize a new instance of the filter before each test
  // var regexFilter;
  // beforeEach(inject(function ($filter) {
  //   regexFilter = $filter('regexFilter');
  // }));

  // it('should return the input prefixed with "regexFilter filter:"', function () {
  //   var text = 'angularjs';
  //   expect(regexFilter(text)).toBe('regexFilter filter: ' + text);
  // });

  it('true', function () {
    expect(true).toBe(true);
  });

});
